language: java
jdk:
  - openjdk11

branches:
  only:
    - master #master branch에 push될 때만 ci
before_install:
  - chmod +x gradlew

#TRAVIS-CI 서버의 HOME
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

#CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - mkkw2047@gmail.com