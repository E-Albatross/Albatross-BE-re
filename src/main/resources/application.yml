server:
  #address: 172.31.34.11
  port: 8000

spring:
  profiles:
    active: real
    #active: local
    #include: oauth
  config:
    import:
      - classpath:s3.yml

  jpa:
    properties:
      hibernate:
        format-sql: true
        show-sql: true
  thymeleaf:
    cache: false
  session:
    store-type: jdbc
devtools:
  livereload:
    enabled: true
